// Arguments passed into this controller can be accessed via the `$.args` object directly or:
var args = $.args;


$.touchEnabledFalseButton.addEventListener('click', function (_evt) {
    console.log('touchEnabledFalseButton _evt=' + JSON.stringify(_evt)); 
});