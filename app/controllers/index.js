var search = Titanium.UI.createSearchBar({
    barColor:'#000', 
    showCancel: true,
    width: 350,
    height: 143,
    top: 0,
    borderColor: 'red',
    borderWidth: 5
});

$.container.add(search);


$.container.open();
